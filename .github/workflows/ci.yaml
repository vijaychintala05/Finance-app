name: FirmBooks CI - Production Qualification

on:
  push:
    branches:
      - main
      - master
      - "feature/**"
      - "release/**"
  pull_request:
    branches:
      - main
      - master

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  static-analysis:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: TypeScript Lint & Compilation Check
        run: npm run lint

  test-suites:
    name: PostgreSQL & Backend Regression Suites
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: firmbooks_test
          POSTGRES_USER: firmbooks_test
          POSTGRES_PASSWORD: firmbooks_test_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run Full Vitest Regression Suite
        env:
          DATABASE_URL: postgres://firmbooks_test:firmbooks_test_password@127.0.0.1:5432/firmbooks_test
          JWT_SECRET: test-ci-secret-32-chars-long-security
          APP_ENCRYPTION_KEY: test-encryption-key-for-ci-suite-32ch
        run: npm test

  browser-qualification:
    name: Playwright Browser Qualification
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: firmbooks_e2e
          POSTGRES_USER: firmbooks_e2e
          POSTGRES_PASSWORD: firmbooks_e2e_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium

      - name: Build Production Client & Server
        run: npm run build

      - name: Run Playwright Browser Regression Specs
        env:
          DATABASE_URL: postgres://firmbooks_e2e:firmbooks_e2e_password@127.0.0.1:5432/firmbooks_e2e
          JWT_SECRET: test-e2e-secret-32-chars-long-security
          APP_ENCRYPTION_KEY: test-encryption-key-for-e2e-suite-32ch
        run: npx playwright test

      - name: Upload E2E Test Artifacts on Failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

  release-build:
    name: Production Artifact Build Verification
    runs-on: ubuntu-latest
    needs: [static-analysis, test-suites]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build Production Assets
        run: npm run build
