# FirmBooks vs Zoho Books India: Point-by-Point Module Scorecard

Date: 2026-09-22  
Evidence base: current `main` worktree at `20635ea`, source inspection, capability gates, route ownership, test inventory, architecture documents, and the 2026-09-21 QA report.  
Status: static audit in progress. Scores are diagnostic, not production certification.

## Scoring and evidence rules

- **8-10:** coherent, server-backed workflow with strong accounting/security/test evidence; remaining work is polish, production qualification, or breadth.
- **5-7:** useful implementation exists but has material UX, ownership, evidence, deployment, or edge-case gaps.
- **2-4:** partial/prototype/supporting primitives; not competitive as a complete workflow.
- **0-1:** absent or explicitly out of scope.
- **Core:** source-enabled in production. **Certified optional:** code-reviewed but deployment-gated. **Partial:** code exists without a complete or consistently exposed contract. **Absent:** no product workflow.

Every future “done” claim must identify the commit, configuration, database mode, command, browser evidence, and target deployment. A route, component, test name, or service alone does not prove a product capability.

## 1. Dashboard, navigation, and global work

| Workflow | State | Score | Evidence and gaps | Highest-value improvement |
|---|---|---:|---|---|
| Role-aware dashboard | Partial/in flight | 6 | Server-backed summary, permissions, attention data, ledger widgets, mobile tests, and recent browser QA exist. The mobile summary no longer derives “Cash In Hand” from potentially stale client balances or account-name guesses; it presents the server’s liquid cash-and-bank ledger total with the response as-of date and keyboard-accessible banking navigation. The mobile overview now puts the server-provided, permission-filtered attention queue before financial summary cards; focused dashboard tests pass (26), the mobile/desktop attention-first browser checks pass (2), and TypeScript and production build pass. `DashboardView.tsx` remains very large; desktop hierarchy, repeated context, and broader basis/freshness/drill-down consistency remain open. | Extend the action-first hierarchy to desktop; split shell/widgets/query state; make every figure disclose basis, period, freshness, and drill-down destination. |
| Global navigation | Core | 8 | Desktop and mobile now consume one typed route inventory, including estimates, sales orders, purchase orders, delivery challans, salespersons, reconciliation, security, and identity. Capability-owned routes remain visible but disabled with a deployment explanation, and direct links use the same gate. Global-search record navigation and a validated origin breadcrumb now use the route contract; quick create and permission metadata remain separate. | Extend the route registry into search, breadcrumbs, quick create, permissions, and preserved return context. |
| Global search | Core | 7 | Tenant- and permission-filtered server search and ArrowUp/ArrowDown/Enter navigation exist. The palette exposes dialog/combobox/listbox semantics and up to five in-memory recent records. Results use an explicit category-to-supported-view allowlist, carry the active organization, ignore API route strings, and preserve a validated internal origin. Expense opens its supported exact view; unsupported categories stay visible but non-navigable. Missing/oversized IDs are rejected; organization changes clear recents, in-flight results, and route context. Four focused search/route/report suites pass 61 tests; lint and production build pass. Safe previews, saved search, and a broader command layer remain open. | Add safe entity previews, saved searches, and consistent commands. |
| Quick create | Core/partial | 6 | Multiple create paths exist and are tested, but availability and post-save behavior vary by module. | Use one capability/permission registry, consistent drafts, validation summary, pending state, success receipt, and committed-but-refresh-failed recovery. |
| Cross-module continuity | Partial | 6 | A typed hash-route parser/builder allowlists tabs/reports, bounds IDs/filter text/page values, validates dates/status, and supports nonrecursive internal tab, record, and report return routes. Report drill-down preserves report context; global search uses explicit exact-view mappings and preserves its validated origin. Browser Back/Forward, detail close, and organization-switch cleanup are covered. Focused search/route/report coverage passes (61 tests); O2C browser drill-down/return passes desktop/mobile (2/2) against the local in-memory test server; lint and production build pass. Customer portal tokens remain separate; saved views, quick create, permission metadata, and remaining module links are still outside this contract. | Extend typed route context to saved views, quick create, permissions, and remaining module links. |

## 2. Organization, identity, security, and access

| Workflow | State | Score | Evidence and gaps | Highest-value improvement |
|---|---|---:|---|---|
| Registration and organization provisioning | Core | 8 | Atomic tenant/control-account provisioning, supported base currencies, auth safety tests, and organization switching exist. | Add an explicit onboarding checklist and migration path; measure time to first posted invoice and first reconciled bank statement. |
| Login, refresh, logout, password recovery | Core/partial | 7 | JWT/cookie/session controls, rate limits, and expiry checks exist. Password recovery intentionally fails closed until verified email delivery is configured. | Complete email-backed one-time recovery; unify JWT and opaque-session lifecycle; expose clear device/session consequences. |
| MFA and recovery codes | Core | 8 | TOTP/recovery workflows and security tests exist. | Add enforce-by-role/org policy, trusted-device rules if desired, recovery-code rotation, and administrator visibility without exposing secrets. |
| Sessions and device management | Core | 7 | Access JWTs carry a signed session ID and middleware validates the matching active, unexpired user-owned row atomically; refresh preserves it and targeted/all-other revocation invalidate those credentials. Password login and MFA completion recheck credential state under a user-row lock before session creation; password changes use the same lock, update both password stores, and revoke all sessions/tokens transactionally. MFA tickets carry an HMAC credential-state proof. Session-event writes commit with row changes. The list DTO omits user IDs/token hashes; the UI shows real/unknown IP values, request outcomes, and session-refresh errors without stale controls. Focused 39 service/API and 3 UI tests, lint, and production build pass; deployment forces one-time sign-in for legacy unbound JWTs. PostgreSQL concurrency/audit-failure qualification remains open. | Qualify row-lock interleavings and event rollback on PostgreSQL; add device-specific alert/email evidence and account-recovery coverage. |
| Users, invitations, memberships | Certified optional | 8 | The audited membership service is now the only client path: unused alert-only membership/session/governance facade methods were removed. Team Access shows active/pending counts, full invitation lifecycle state, explicit expiry, least-privilege roles, guarded role/revoke confirmations, session-invalidation consequences, persistent receipts, and uncertain/committed-but-refresh-failed recovery. | Add resend with delivery evidence, ownership-transfer acceptance, role-permission preview, and production email qualification. |
| Roles, permissions, separation of duties | Core/partial | 8 | Extensive permission registry, custom roles, approval boundaries, SoD metadata, and tests exist. Custom-role deletion now has an accessible confirmation, durable organization/role/idempotency recovery, exact success receipts, atomic delete plus strict audit, and serialized assignment/invitation writes. Uncertain outcomes are verified against an authoritative role list; list-read failures and unverified 404 responses preserve the retry guard. Six focused suites pass (78 tests); TypeScript lint, production build, and Sol 6 review pass. OR-semantics and legacy mappings still increase reasoning cost. | Generate one permission catalog for UI and API; show effective permission explanations; retire legacy aliases; enforce and report SoD conflicts. |
| Organization governance | Partial | 6 | Settings, audit, immutable base currency after posting, and recovery locks exist; status/ownership changes remain intentionally guarded or incomplete. | Add audited ownership transfer and organization lifecycle with dual confirmation, recovery plan, and no ambiguous destructive actions. |

## 3. Master data

| Workflow | State | Score | Evidence and gaps | Highest-value improvement |
|---|---|---:|---|---|
| Customers | Core/partial | 8 | Customer metadata edit and soft archive now use tenant-scoped, permission-guarded server mutations that synchronize the canonical customer and client projection, preserve financial history, and return request-aware UI receipts. Archived customers leave active lists and are barred from new business while existing records remain. Clients-only legacy rows get an explicit 409 until their canonical record is restored. | Add duplicate detection and guarded merge, legacy-row canonicalization, archived-history navigation, activity timeline, and field-specific validation errors. |
| Vendors | Core | 7 | Vendor CRUD/workspace, statements, P2P documents, payments, and status handling exist. Create/edit now warn without blocking on exact normalized GSTIN, PAN, or email matches against the canonical vendor directory, excluding the current record. Vendor workspace PO rows now open the selected order in the authoritative PO detail and conversion flow. Vendor documents load from the permission-checked attachment API on workspace open; removal uses the audited archive endpoint behind an accessible confirmation with request-ID feedback. Focused vendor UI coverage passes (2 files, 8 tests). Desktop/mobile Playwright passes against the local in-memory server and verifies API-backed attachment persistence across reload plus archived removal after reload; PostgreSQL attachment qualification remains open. | Decompose workspace, add guarded merge/archive, and decide vendor portal readiness. |
| Items and services | Core/partial | 6 | Item master, quotation/invoice/purchase-document use, GST fields, and tests exist. The catalog has tenant-scoped view/create/edit/archive grants, organization-aware writes, fail-closed custom-role resolution, audited soft archive, and unchanged-request retry recovery. Focused catalog view/API coverage passes 59 tests across 2 files, and lint passes. Its header states that SKU/unit are document references and that on-hand stock, inventory valuation, warehouse availability, and reorder levels are not tracked. No stock ledger or batch/serial workflow exists. | Choose and certify a separate inventory subledger before exposing stock quantities, valuation, warehouse availability, or reorder behavior. |
| Salespersons | Core/partial | 6 | Server-backed tenant-scoped create/edit/archive/restore, granular permissions, strict audit writes, stale-write protection, active-customer archive guard, and customer-default invoice attribution are implemented. Invoices store immutable salesperson snapshots; the sales report aggregates one row per stored ID using the current master name and marks null IDs Unassigned. Focused salesperson API/UI coverage passes (5 tests), adjacent report/context coverage passes (38 tests), Sol 6 final review PASS; typecheck and production build pass. The directory uses an accessible deactivation confirmation with visible API failure feedback. PostgreSQL migration/index and row-lock qualification remain open. | Qualify legacy-code backfill, normalized code uniqueness, and assignment/archive concurrency on a confirmed disposable PostgreSQL database; then complete an end-to-end live UI audit. |
| Chart-linked defaults | Core | 7 | Account defaults and server provisioning exist; fixed control codes and configurable mappings coexist. | Complete the migration from magic control codes to validated organization mappings and surface configuration health. |

## 4. Sales and receivables

| Workflow | State | Score | Evidence and gaps | Highest-value improvement |
|---|---|---:|---|---|
| Quotations/estimates | Core | 8 | Server engine, revisions, PDF, GST, public response token, conversion, and tests exist. The dirty-close flow now uses an accessible in-app discard dialog with focus moved to “Keep editing”; the quotation UI suites pass (3 files, 62 tests). | Standardize workspace shell, approval/send timeline, expiry/reminders, edit conflict handling, and conversion provenance. |
| Sales orders | Core/in progress | 7 | Partial fulfillment/invoicing and lineage exist. The UI has reason-required audited cancellation and tenant-bound recovery. Backend posting/void/fulfillment/cancellation now share sales-order-first locking, enforce terminal-state and exact-cent limits, inspect linked invoice/challan evidence, preserve invoice/fulfillment state, reject linked invoice edits and caller-supplied conversion lines, and route linked challans through authoritative issued fulfillment. Idempotent retry permissions and unexpected-failure 5xx handling are covered. GST-bearing partial conversion and challan cancellation remain unsupported; PostgreSQL qualification remains open. Final Sol review PASS; focused regressions, build, and lint pass. | Model installment GST allocation and audited challan cancellation, then qualify the full lifecycle against PostgreSQL. |
| Delivery challans | Certified optional | 7 | Standalone create is now clearly draft-only, persisted status fields are normalized for display, and list details replace the stale alert-only delivered action. Navigation remains indirect and fulfillment mutation is restricted to the audited sales-order workflow. | Add clear status/lineage workspace, print/e-way-bill boundary, partial delivery handling, and certified reversal/cancellation UX. |
| Invoices | Core | 8 | Posting, approvals, edits/revisions, PDFs, email, reminders, payments, write-offs, and unbilled costs exist. Invoice void now persists an exact user/org/key/reason guard before POST and restores it from sessionStorage after same-tab reload. A scoped status route recomputes the canonical request hash and verifies the saved receipt, invoice state, posted reversal links, and matching audit row; unknown/in-progress/conflict states keep financial actions paused, exact same-request retry is available for unresolved pre-commit failures, and deterministic 4xx versus unexpected rollback 5xx semantics are preserved. Lint and production build pass; focused recovery/accounting coverage passes 106 tests across 8 files plus 31 security/invoice regressions across 3 files; the desktop/mobile audited void-and-reload journey passes 2/2. Invoice void now appends its optional expense-release event and mandatory INVOICE_VOIDED event as a timestamp-ordered hash batch inside the existing financial transaction. The saved receipt contains the exact audit ID; status recovery recomputes that entry hash and checks its predecessor link. An injected audit failure proves invoice and reversal writes roll back together, and the linked-expense regression verifies both event hashes and ordering. The focused invoice operation, audit-chain, recovery UI, and lifecycle bundle passes 82 tests across 8 files. Legacy unhashed rows remain explicit gaps, so organization-wide chain integrity is not claimed; PostgreSQL concurrency and JSONB qualification remain open. Invoice create now persists a frozen org/user/payload/key/hash receipt before POST, validates the exact 201 command and journal before reporting success, and recovers through tenant/user/permission-scoped checks of the idempotency record, immutable financial command, invoice, original posted journal, and evidence link. It preserves SUBMITTED approval outcomes without pretending they are posted, blocks identity changes during hash computation, and keeps the approval notice through refresh failure. The in-memory API regression verifies exact replay creates one financial command; focused API/service/context/client tests pass (77), TypeScript and production build pass, and final Sol review is PASS. PostgreSQL qualification remains unrun because automatic review blocked the database command when the target was not established as local or disposable. The create receipt stores the original posting journal ID and offers exact original-journal navigation only for committed POSTED receipts. Invoice editing now uses a tenant-scoped BIGINT edit version: successful edits and other invoice mutations advance it; update requests require the token and stale drafts receive a 409 with current server values before any side effect. The editor preserves the draft, shows dates, amounts, discount, notes, terms, salesperson/project, and line quantities/prices/tax, then requires explicit rebase and a second save. Missing and malformed tokens return 428 and 400. Focused stale-update, payment invalidation, revision atomicity, recurring-version, and rebase UI tests pass; TypeScript, production build, and final Sol review pass. PostgreSQL migration and concurrent-transaction qualification remain open. The explicit-ID route requires a tenant-scoped invoice, posted journal, matching POSTED_TO evidence link, and completed invoice.post result; it fails closed if drill-down resolves another journal. The journal UI labels the original only after its loaded ID matches and ignores stale out-of-order responses. Revision, mismatch, tenant, missing-evidence, switch, race, and SUBMITTED regressions pass; the focused bundle passes 80 tests across 5 files, lint/build pass, and final Sol review is PASS. Fresh continuation verification passes 81 receipt/void tests across eight client/server files, and the create-receipt to exact-original-journal to audited-void/reload Playwright path passes desktop and mobile (2/2) against the local in-memory server. | Invoice email now renders and stores an immutable PDF snapshot in the same transaction as canonical DRAFT posting, explicit invoice/message linkage, outbox insertion, and audit evidence. Same-key retries are covered by the shared finance idempotency middleware; history is tenant-scoped and exposes queued, processing, retrying, accepted, failed, dead-letter, and suppressed states without internal payload/errors. Sends/reminders now report QUEUED, suppress voided or settled pending messages, fail when SMTP is unavailable, and escape invoice email content. The focused delivery bundle passes 66 tests across six files; lint and production build pass, and final GPT-6 Sol review is PASS. PostgreSQL migration and transaction qualification remain open. Qualify the invoice edit-version migration and concurrent edit/payment behavior, plus audited create and void journeys, against PostgreSQL CI or a confirmed disposable database. |
| Payments received | Core | 8 | Allocation, advances, and server-owned audited reversals exist. The UI shows each invoice allocation and unapplied remainder from tenant-scoped server data. Reversal preserves the current available balance as zero while recording the pre-reversal unapplied amount in the same transaction; legacy v15 recovery artifacts upgrade with the historical amount marked unknown. Focused recovery/payment/UI/schema suites pass (31 tests), and final Sol review is PASS. PostgreSQL migration qualification remains open. | Add provider reconciliation and settlement status, then qualify reversal persistence against PostgreSQL before claiming online-payment parity. |
| Credits, refunds, write-offs, advances | Certified optional | 7 | Server-backed correction families and reversal tests exist, deployment-gated. | Consolidate them into one receivables settlement workspace with policy-driven valid actions, linked evidence, and deployment readiness status. |
| Recurring invoices | Certified optional | 8 | One authoritative recurring workspace now owns creation, pause/resume, next run, durable occurrence status, attempt count, generated-invoice lineage/drill-down, quarantine cause, permission-guarded retry, and uncertain/committed-but-refresh-failed receipts. The obsolete alert-only facade and duplicate invoice screens are removed. | Add worker health/last heartbeat, failure notification policy, and production scheduler qualification. |
| Customer portal and online payment | Partial | 6 | Token portal, invoice viewing, checkout initiation, provider adapters, and tests exist. The standalone administrator route now correctly loads its customer selector from the canonical array API and is verified at desktop/mobile widths. Blocking alerts, invitation/activity history, and production provider qualification remain separate. | Add customer identity/session option, invitation and portal activity history, statement/download/payment history, accessible inline errors, and provider settlement/reconciliation evidence. |

## 5. Purchases and payables

| Workflow | State | Score | Evidence and gaps | Highest-value improvement |
|---|---|---:|---|---|
| Purchase orders | Core | 7 | Create/update/approve/receive/convert/cancel routes, partial billing, lineage, and tests exist. Creation, receiving, conversion, and reason-required cancellation now share pending guards and persistent success/rejected/uncertain/committed-but-refresh-failed receipts with request IDs; vendor identity is ID-based. | Use the common document shell and show ordered/received/billed quantities, remaining value, approval evidence, and direct linked-document navigation. |
| Goods/service receipts | Core/partial | 6 | Server lifecycle and tests exist, but it is not a strong first-class workspace in navigation. | Add receiving workspace, partial/over receipt rules, attachments, quality exceptions if needed, and direct PO/bill lineage. |
| Bills | Core | 8 | Posting, approvals, updates, voids, taxes, payments, and reconciliation tests exist. Bill details now mirror the invoice reason-required reversal dialog and preserve success, structured failure, uncertain-outcome, and request-ID evidence. | Add duplicate/vendor-invoice detection, evidence completeness, editor conflict recovery, and browser qualification of reversal persistence. |
| Vendor payments and advances | Certified optional | 9 | The authoritative settlement workspace exposes server-backed allocations, advances, applications, immutable record evidence, accessible reversal confirmation, and success/rejected/uncertain/committed-but-refresh-failed receipts. Report/search deep links open the owning record. The vendor-bill-to-payment journey is part of the PostgreSQL browser release gate. Production exposure remains capability-controlled. | Add multi-bill allocation editing and approval-queue evidence. |
| Vendor credits, debit notes, refunds, write-offs | Certified optional/partial | 8 | One settlement contract now owns debit notes, source-linked vendor refunds, reversals, evidence drill-down, and separately gated write-offs. Dead duplicate screens and alert-only facade mutations are removed; vendor workspaces read authoritative debit notes instead of an empty browser collection. | Extend PostgreSQL browser qualification from payment settlement to refund/write-off/reversal journeys and add refund-source remaining-balance previews. |
| Recurring bills and expenses | Certified optional | 8 | Bills and expenses use the same authoritative recurring workspace, occurrence evidence, guarded quarantine retry, and generated-record drill-down as invoices; obsolete facade methods and duplicate screens are removed. Vendor recurring-bill history now reads the server profiles API rather than an always-empty browser cache. | Add worker health/alerting and production scheduler qualification. |
| Vendor portal | Absent | 1 | No vendor self-service workspace comparable to the customer portal. | Decide whether the target buyer needs PO acknowledgement, invoice upload, payment status, and statements; partner/build only after security model is defined. |

## 6. Expenses, evidence, and employee spend

| Workflow | State | Score | Evidence and gaps | Highest-value improvement |
|---|---|---:|---|---|
| Direct expenses | Core | 8 | Posting, receipts, updates/corrections/voids, PDFs, billable conversion, mobile UI, and tests exist. Detail conversion, voucher, receipt, and void actions now use persistent request-aware receipts; voiding requires an audit reason and preserves reversal history. | Consolidate create/detail state; show immutable posting evidence and correction lineage; improve the create form's accounting preview. |
| Receipt attachments | Core | 8 | Image bounds, preview, persistence, reopening, and QA evidence exist. Base64-in-PostgreSQL is not a scalable document architecture. | Move binary evidence behind `StorageProvider`, store metadata/checksums in DB, scan content, generate thumbnails, and stream securely. |
| Document inbox and OCR | Partial | 5 | Inbox UI, OCR service, and APIs exist, but external OCR is not part of the certified baseline and failure/privacy economics are unclear. | Treat as assisted draft extraction only; show confidence per field, human approval, provider/data-retention controls, and never auto-post. |
| Employee claims/reimbursements | Core/partial | 7 | Claim lifecycle, approval, payment, void, treasury integration, and tests exist. Discoverability in the UI is weaker than the server capability. | Add a first-class employee spend workspace with policy checks, approver queue, receipt completeness, mileage/per-diem only if demanded. |
| Billable expenses and cost recovery | Core | 8 | Project/client association, markup, invoice selection/conversion, lineage, and tests exist. | Make recovery status visible from expense, project, customer, and invoice; protect internal cost/markup from portal/PDF leakage. |

## 7. Banking and cash

| Workflow | State | Score | Evidence and gaps | Highest-value improvement |
|---|---|---:|---|---|
| Bank/cash account management | Core | 8 | Core-enabled account lifecycle, GL linkage, account workspace, and tests exist. The 390 px account-card action row now preserves whole labels and 40 px controls. A responsive coverage guide excludes petty cash and puts the first reconciliation action ahead of passive totals on mobile. | Add opening/setup health, archive/close workflow, import history, and clearer distinction between book and bank balance. |
| Statement import | Certified optional | 9 | The certified boundary accepts CSV, XLSX, and text/HTML XLS exports; true legacy binary XLS is rejected safely. Client and server share one format contract. ExcelJS ingestion is guarded by archive, expansion, encryption, ZIP64, row, column, and cell limits. Preview/confirm, actual source provenance, fingerprints, fuzz, real-world tests, the first-reconciliation guide, and audited duplicate resolution exist. | Add import-batch history and parser-rejection UX, strengthen durable provenance, and qualify dormant structured-format parsers before exposing them. |
| Matching and categorization | Certified optional | 8 | Match engine, rules, bulk/partial flows, categorization, reversals, and E2E exist. | Explain match confidence, support split/many-to-many review clearly, and maintain a complete evidence trail. |
| Reconciliation | Certified optional | 8 | Sessions, complete/reopen, verification, reports, permissions, and E2E exist. The critical reconciliation journey is now included in PostgreSQL CI and exact-image pre-publish qualification. | Add attachment support, close readiness integration, exception aging, and retained production-scale evidence. |
| Bank rules | Certified optional/partial | 6 | Server rule CRUD and engine exist. UI/analytics and safe automation controls are less evident. | Add rule simulation, priority/conflict detection, approval threshold, audit/version history, and “never auto-post” default. |
| Connected bank feeds | Prototype | 3 | `BankFeedSyncService` has a mock provider; no certified real provider connection, OAuth lifecycle, consent, or provider operations. | Partner with a bank-feed aggregator; implement connection health, consent renewal, cursor/idempotency, outage handling, and reconciliation-first import. |
| Treasury movements | Core | 7 | Payroll/reimbursement/owner/loan/tax movements post durable source records and journals. | Integrate into a coherent cash workspace, add approval policies and evidence, and avoid presenting this as full payroll or tax-payment automation. |

## 8. Accounting, close, and compliance

| Workflow | State | Score | Evidence and gaps | Highest-value improvement |
|---|---|---:|---|---|
| Chart of accounts and subaccounts | Core | 8 | Hierarchy, defaults, validation, locks, and tests exist. Archive/restore/delete use accessible confirmations and persistent user-bound recovery guards. Guard ownership binds to AuthContext’s verified session revision and credential; token changes, stale profile responses, and A→B sign-in transitions fail closed, and only the initiating user can verify or clear. Legacy guards without an owner remain blocked for manual server-audit reconciliation. Account creation returns a committed receipt, verifies the exact saved row, and blocks repeated creates when refresh is stale; account-only read tickets prevent older mount/manual/action-verification responses from overwriting newer account state while unrelated mount domains still refresh. Quick Add preserves request metadata and distinguishes deterministic bank rejection from uncertainty. Seven focused account/auth/bank setup suites pass (98 tests); typecheck/build pass; final Sol 6 review PASS. Components are large and some category/control-code semantics overlap. | Add account-health checks, merge/archive workflow, mapping migration, usage impact preview, and smaller domain components. |
| Manual and bulk journals | Core | 8 | Balanced posting, approvals, bulk entry, locks, reversals, and tests exist. | Standardize drafts, templates, attachments, recurring handoff, import validation, and line-level error navigation. |
| Transaction approvals | Core | 8 | Rules, requests, decisions, consumption, self-approval controls, and tests exist. Coverage differs by document family. | Publish the approval coverage matrix in-product; ensure every supported financial command uses the same registry and state language. |
| Period locks | Core | 8 | Server-enforced locks and UI exist. | Add scoped locks, impact preview, exception workflow, and link lock decisions to close evidence and audit trails. |
| Period close/reopen | Certified optional | 7 | Validation, workspace, review, close/reopen, audit, and E2E exist. Prior architecture notes say earnings transfer and universal close semantics remain limited. | Define a jurisdiction/accounting close checklist, retained-earnings policy, sign-off package, and immutable close evidence bundle. |
| Fixed assets | Certified optional | 7 | Create, depreciate, dispose, reverse, register report, and tests exist. | Add asset classes, schedules, componentization, impairment/transfer only if demanded, and close integration. |
| GST evidence and calculation | Core/partial | 6 | CGST/SGST/IGST, slabs, GST summaries, cross-validation, and HSN/SAC-related fields exist. It is explicitly not statutory filing integration. | Harden place-of-supply/registration/master-data validation and produce return-ready exception queues with accountant sign-off. |
| GST filing, e-invoice, e-way bill, IMS | Absent/partner decision | 1 | No direct GST portal/IRP filing workflow. This is a major Zoho Books India gap. | Decide the product position; if parity is required, integrate certified GSP APIs with credential vaulting, retries, reconciliation, cancellation, and regulatory update ownership. |
| Multi-currency transactions | Partial primitives | 3 | Multiple base currencies and an FX revaluation service exist, but the capability matrix excludes foreign-currency transaction conversion and settlements. | Design currency-aware document amounts, exchange-rate sources, realized/unrealized gains, revaluation, reports, and migration before exposing export templates as capability. |

## 9. Projects and time

| Workflow | State | Score | Evidence and gaps | Highest-value improvement |
|---|---|---:|---|---|
| Project master and workspace | Core/strong partial | 7 | Create, edit, and archive now use tenant-scoped audited server mutations with request receipts, case-insensitive duplicate handling, immutable linked code/customer identity, persisted start dates, and history-preserving archive. Archived assignments are blocked across new work; old unbilled time remains billable through the atomic audited settlement path. Project lifecycle, date serialization, legacy customer conversion, link freeze, and UI uncertainty regressions pass. | Add project activity timeline, fuller workspace reporting/ownership views, and continue replacing remaining blocking dialogs. |
| Time tracking | Core/partial | 7 | Time-log deletion uses the audited server response (including HTTP 204), an accessible confirmation, and a durable receipt with uncertain and committed-but-stale retry protection. Unbilled-time conversion reports the posted invoice and no longer opens a second create form. Time-entry creation persists its exact idempotency key with the frozen payload and organization, and can confirm an uncertain commit only from a same-user, same-organization, unexpired server receipt backed by the original create permission. Payload matches in Time Logs never clear the guard; exact replay reuses the persisted key. Committed-but-stale timer receipts remain visible after remount. Focused client/provider/server coverage passes (7 files, 78 tests); server accounting tests remain authoritative for atomic invoice and time-entry posting. Dashboard timer persists in localStorage and is not authoritative across devices. | Move active timer/session state server-side; add approvals, locking, conflict recovery, and mobile-safe start/stop. |
| Unbilled time and expenses | Core | 8 | Selection, markup, invoice integration, project/customer views, and tests exist. | Provide one unbilled queue with aging, ownership, bulk review, leakage protections, and traceable invoice conversion. |
| Project profitability | Core | 7 | Dedicated server report and tests exist. | Clarify cost basis, allocations, WIP, period cutoffs, cash vs accrual, and drill down from every total. |

## 10. Reports and analytics

| Workflow | State | Score | Evidence and gaps | Highest-value improvement |
|---|---|---:|---|---|
| Financial statements | Core | 8 | P&L, balance sheet, trial balance, general ledger, cash flow, comparisons, and reconciliations exist with authoritative renderers. | Add consistent drill-down, accounting basis disclosure, comparative presets, close snapshots, and export parity. |
| Operational report catalog | Core/partial | 7 | UI lists 38 reports; 31 use `ReportWorkspaceService` and the remainder use authoritative services. Several workspace calculations use generic `Number` conversion and one large service owns all reports. | Replace float helpers with exact money; split by domain; give each report ownership, query budget, lineage, permission, and certification status. |
| Filters and drill-down | Partial | 6 | Date/entity/status/search filters and some ledger drill-downs exist; coverage varies by report. | Define a universal filter contract, shareable URLs, source-document drill-down, back-stack preservation, and unsupported-filter errors. |
| Saved views and favorites | Partial | 6 | Saved report views remain server-backed. Catalog favorites now persist in browser storage scoped to the signed-in user and active organization; the focused Reports workspace suite passes (5 tests), and TypeScript lint passes. Favorites do not sync across devices; visible columns, filters, sharing, ownership, and last-run metadata still need consistent persistence. | Move catalog favorites to an authenticated server-backed preference and persist the remaining view state consistently. |
| Scheduled/shared reports | Absent/partial | 2 | Email outbox exists, but report schedules, subscriptions, delivery history, and access-aware sharing are not established. | Add scheduled report definitions, immutable parameters, recipient authorization, delivery evidence, retries, and revocation. |
| Advanced analytics/custom reports | Absent/partial | 3 | Fixed catalog, budgets/forecast gates, and charts exist; no safe semantic custom-report builder or Zoho Analytics-class BI. | First expose a versioned reporting semantic layer; then add safe dimensions/measures or integrate an external BI tool read-only. |

## 11. Documents, communication, portals, migration, and recovery

| Workflow | State | Score | Evidence and gaps | Highest-value improvement |
|---|---|---:|---|---|
| PDF/document templates | Core | 8 | Versioned templates, branding, historical snapshots, preview, multiple financial documents, responsive settings, and tests exist. `PdfTemplatesSettings.tsx` is 2,800+ lines. | Break into schema/editor/preview/persistence modules; add accessibility, template migration, visual regression, and only show fields supported by document data. |
| Email and reminders | Core/partial | 7 | Invoice send/reminders and durable email outbox/retries exist. Broader automation and delivery analytics are limited. | Centralize communication templates, sender policy, bounce/delivery state, customer preferences, retries, and document timeline events. |
| Customer portal | Partial | 7 | Token access, documents, checkout, payment status, admin customer selection, accessible revocation confirmation, persistent operation feedback, and tests exist. | Add authenticated invitation delivery, token/activity history, secure document center, customer identity/session options, and production-provider evidence. |
| Vendor portal | Absent | 1 | No equivalent workflow. | Only build if the buyer needs supplier collaboration; begin with PO acknowledgement, invoice upload, and payment status. |
| Data migration/opening balances | Partial | 6 | Server migration service, modal, schemas, and tests exist. | Build a staged mapping/validation/reconciliation wizard with resumability, rollback, provenance, and supported-source templates. |
| Data export | Certified optional/partial | 6 | Export services and guarded endpoints exist. | Publish an audited portable export contract, checksums, encryption, completeness manifest, and restore compatibility. |
| Backup and recovery | Certified optional | 8 | Encrypted artifacts, staging, validation, promotion, locks, recovery tests, runbooks, and center UI exist. Deployment keys/config remain decisive. | Add scheduled drills, operator evidence, RPO/RTO dashboard, off-device copy policy, alerting, and production restore certification. |
| Audit/security center | Core | 7 | Audit logs, security events, metrics, MFA, and settings views exist. Coverage is a caller responsibility and append-only chain completeness has limits. | Create an audit-coverage registry per command, tamper-evidence health, export/sign-off, alert triage, and request/document drill-down. |

## 12. Platform, architecture, quality, and developer experience

| Area | State | Score | Evidence and gaps | Highest-value improvement |
|---|---|---:|---|---|
| Financial command ownership | Partial | 5 | Posting and command services exist, but direct controller, engine, legacy facade, and newer service paths coexist. | Publish a command registry: owner, input schema, permissions, transaction, idempotency, audit, reversal, tests, feature state, and legacy deletion date. |
| Frontend state/data architecture | Partial | 6 | Domain services still coexist with the oversized `BooksContext` compatibility facade, but unused alert-only identity/access, recurring-document, and payable-correction methods plus six duplicate screens have been removed. Team Access, recurring, and payable corrections now use their authoritative APIs. Many other financial facade mutations remain. | Freeze new facade setters; move each remaining domain to typed query/mutation hooks; delete stale placeholders after caller migration. |
| Backend modularity | Partial | 5 | Strong domain logic exists inside 2,000-4,000 line controllers/engines. | Split by command/query/document family behind stable contracts; keep transactions at use-case boundaries; add dependency rules. |
| Database/migrations | Core | 7 | Transactional accumulated DDL, advisory locking, constraints, schema version, and PostgreSQL CI exist. Migrations are not independently reversible/versioned files and RLS is partial/permissive when context is absent. | Move toward explicit ordered migrations, force/expand RLS carefully, verify context on every pool path, add forward/backward compatibility and restore tests. |
| API consistency | Partial | 6 | Auth, org, permission, error middleware, correlation, and idempotency exist. The browser client now sends a trace ID on every request and preserves request ID, code, retryability, cause, fix, recovery, documentation link, and current-state metadata from structured failures. Route aliases, controller-local error shapes, OR-permission semantics, and large controllers remain inconsistent. | Move controller-local errors behind the standard domain envelope; generate client types; add OpenAPI/internal contract tests; retire aliases deliberately. |
| Error experience | Partial | 7 | A shared operation-receipt contract and accessible banner now distinguish confirmed success, deterministic rejection, uncertain mutation outcome, and committed-but-stale refresh state while exposing server recovery guidance and request IDs. Team Access, recurring transactions, settlements (including customer payment reversal), invoice/bill corrections, purchase orders, sales-order creation, expenses, customers, and project creation use it. A fresh frontend source scan finds 16 `window.alert` calls and 1 `window.prompt`, all in the shared `BooksContext` facade. They include committed-but-refresh-failed writes, unsupported actions, and credit-note reversal reason capture; preserve those outcomes and audit reasons when moving them to shared in-app feedback. Item archive now uses an accessible confirmation and retry-safe receipt. | Replace facade-owned blocking messages with shared in-app feedback and receipts; add field-error mapping and copyable support evidence. |
| Performance/scalability | Partial | 6 | Compression, indexes, caches, batch tests, and performance plans exist. Large list/report components and base64 receipts are risks; SLO evidence is limited. | Define datasets and SLOs per workspace; capture query counts/p95; paginate/stream; move blobs; eliminate N+1 and oversized payloads. |
| Observability and jobs | Partial | 6 | Correlation IDs, metrics controller, operational monitoring, outbox, recurring scheduler, and tests exist. In-process timers and production alert wiring remain concerns. | Durable external worker/lease ownership, queue dashboards, dead-letter replay, SLO alerts, and runbooks tied to command IDs. |
| Security | Partial/strong foundation | 8 | Auth, MFA, rate limits, CSP, tenant filters, permissions, webhook checks, recovery encryption, secure spreadsheet boundaries, zero known npm advisories, and security tests exist. RLS is partial and external independent assessment is pending. | Complete tenant-boundary verification, secret rotation, dependency/SAST/DAST gates, and independent penetration review. |
| Test and CI system | Strong | 9 | 1,400+ tests, property/fuzz/concurrency/recovery suites, build, and lint exist. Playwright CI now runs desktop and mobile projects against a PostgreSQL service and fails closed if memory mode is selected; release build depends on that qualification. The publish workflow runs five critical desktop journeys against the exact release container and PostgreSQL before registry publication. | Retain success evidence manifests, add flake/quarantine metrics, expand exact-image mobile/accessibility coverage, and independently verify the first hosted CI run. |
| Developer experience | Partial | 5 | README and commands exist, but setup needs Node/PostgreSQL/manual env; docs are numerous and sometimes stale/conflicting. | One-command disposable dev stack, seeded personas, <5-minute hello world, generated API/schema docs, doc ownership/expiry, and actionable errors. |
| Mobile/accessibility | Partial | 7 | Responsive web audits cover 45 routes and key forms; bottom nav/card tables exist. No native/offline experience or physical-device/Safari certification. | Establish accessibility automation plus manual screen-reader/keyboard/zoom checks; test Safari/iOS; define whether PWA/offline capture is strategic. |
| Integrations/public API/webhooks | Partial/absent product | 3 | Internal REST and payment webhooks exist; public versioning, SDKs, broad webhooks, marketplace, and compatibility guarantees are explicitly deferred. | Decide partner strategy; first create a private versioned contract/event model, then expose narrowly with scopes, signatures, replay, rate limits, and audit. |
| Workflow automation | Partial | 4 | Recurring documents, approvals, bank rules, email outbox, and reminders are separate primitives. No unified trigger-condition-action product. | Build a constrained automation registry over certified commands with dry-run, approvals, rate limits, audit, retries, and safe templates. |
| Inventory | Absent | 1 | Accounts and document item lines include inventory concepts, but no stock subledger, valuation, warehouse, fulfillment, or COGS engine. | Treat as a separate product program or integrate Zoho Inventory/partner; do not imply support from account names/templates. |
| Payroll | Absent | 1 | Payroll payment journals and accounts exist, but no employee, attendance, statutory payroll, payslip, or filing product. | Keep treasury payment labeling explicit; partner with payroll software unless this becomes the chosen wedge. |

## Cross-module findings

1. **Capability truth now has a canonical source, but command ownership remains fragmented.** The shared registry owns core/certified/prototype state, labels, deployment requirements, umbrella implications, published API keys, tab gates, and desktop/mobile navigation. Search, quick create, permissions, command owners, and generated documentation still need to consume the contract.
2. **The compatibility facade is now architectural debt.** It contains alert-only methods for workflows that newer services partly or fully implement. Caller-dependent behavior is a direct UX and trust problem.
3. **The strongest differentiator is evidence, not breadth.** The accounting, reversal, recovery, and test systems can beat generic SaaS trust if every visible action is backed by proof and production configuration.
4. **The largest competitive gaps are ecosystem gaps.** Direct GST/IRP filing, real bank feeds, multi-currency documents, supplier collaboration, scheduled reporting, inventory, payroll, integrations, and unified automation are not small UI additions.
5. **UI feedback is inconsistent.** Blocking browser alerts and confirms remain across customer, project, vendor, settings, portal, and other master-data flows. Financial operations need durable, accessible status with retry-safety guidance.
6. **Large files hide ownership.** File size is not itself a defect, but 2,000-4,000 line controllers/engines and 1,000-2,800 line screens correlate with mixed responsibilities and slow confident change.
7. **Reports are broad but need truth hardening.** Thirty-eight catalog entries are competitive, but exact-money handling, query ownership, drill-down consistency, scheduling, sharing, and production evidence matter more than count.

## Provisional priority order

### P0: trust and release blockers

1. ~~Replace or strictly contain vulnerable `xlsx` statement parsing.~~ Resolved: removed SheetJS, bounded ExcelJS ingestion, safe legacy-XLS policy, and zero known npm advisories.
2. ~~Generate the canonical capability and desktop/mobile route registry.~~ Completed with contract tests. Next: add command ownership, permissions, search, quick create, breadcrumbs, and generated docs to the registry.
3. ~~Remove stale alert-only facade paths from workflows with authoritative APIs; hide truly unavailable actions.~~ Completed for Team Access, recurring documents, and payable corrections; separately deployed write-offs are now capability-visible only when enabled.
4. ~~Run critical financial Playwright journeys against PostgreSQL and make release depend on the result.~~ Implemented: CI uses a PostgreSQL service with a fail-closed Playwright configuration, release build depends on browser qualification, and the exact image must pass five critical journeys before publication. The first hosted CI run remains the external confirmation step.
5. Standardize financial error/uncertain-outcome UX and request-ID support. In progress: the API boundary now preserves the full structured error contract and all requests carry trace IDs; one shared receipt model serves Team Access, recurring transactions, settlements, invoice/bill corrections, purchase orders, expense details, customers, and project creation. Remaining: blocking browser `alert`/`confirm` calls are now absent from components; `BooksContext` still contains legacy alert/prompt facades for disabled or unreached operations; normalize controller-local server envelopes.

### P1: daily-workflow quality

1. One workspace shell for sales, purchases, expenses, banking, and accounting documents.
2. Typed URL navigation, preserved context, universal search/command palette, and consistent notifications.
3. Decompose `BooksContext`, `financeController`, sales/purchases engines, report workspace, PDF settings, and the largest screens by business capability.
4. Production-grade recurring/outbox/job operations and report scheduling.
5. Evidence-backed onboarding, migration, and close/reconciliation experiences.

### P2: competitive expansions requiring a product decision

1. GST GSP integration: e-invoice, e-way bill, return/IMS reconciliation.
2. Connected bank feeds.
3. Foreign-currency document and settlement accounting.
4. Vendor portal and broader integrations/API.
5. Inventory and payroll, preferably through partners unless they become the wedge.

## Completion condition for this scorecard

This static scorecard is only the first evidence layer. A final module audit requires a browser pass through every route in representative roles and states, API/database trace for every mutation family, PostgreSQL E2E for critical journeys, performance measurements, accessibility checks, and a deployment-aware capability export.
